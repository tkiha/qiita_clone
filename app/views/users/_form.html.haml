= simple_form_for @user do |f|
  -#　エラー表示の部分が何箇所かあるのでパーシャルにしたいかも
  - if @user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user.errors.count, "error")} prohibited this user from being saved:"
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg

  = f.input :name
  = f.input :email
  = f.input :password
  = f.button :submit
